import { Modal } from '@common';

interface BackupProxyProps extends AntdModalProps {}
const BackupProxy = (props: BackupProxyProps) => {
  return <Modal {...props}>backupProxy</Modal>;
};

export default BackupProxy;
